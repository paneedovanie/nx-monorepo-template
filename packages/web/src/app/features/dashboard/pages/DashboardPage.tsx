import { Box, Typography } from '@mui/material';

export const DashboardPage = () => {
  return (
    <Box sx={{ p: 3, height: '100vh', display: 'flex', alignItems: 'center' }}>
      <Typography>Dashboard</Typography>
    </Box>
  );
};
