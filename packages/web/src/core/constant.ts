const color = {
  primary: '#000000',
  secondary: '#ffffff',
  accent: '#0072c6',
  black: '#000000',
  white: '#FFFFFF',
};

const font = {
  size: {
    sm: '10px',
    md: '12px',
    lg: '14px',
    xl: '20px',
  },
};

const defaultSize = {
  sm: '4px',
  md: '8px',
  lg: '12px',
  xl: '16px',
};

export const theme = {
  color,
  font,
  margin: defaultSize,
  padding: defaultSize,
  radius: defaultSize,
};

export const app = {
  title: 'Users',
};

export const apiBaseUrl = 'http://localhost:3000';
